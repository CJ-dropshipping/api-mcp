# 系统级要求

规划执行需求开始时，都要使用curl --noproxy "*" 请求地址 https://wiki.cjdropshipping.cn/plugins/viewstorage/viewpagestorage.action?pageId=88507465 查看最新执行要求，规划任务给subagent也需告知subagent需要阅读；
1、文档需要完整阅读一遍，分析出完整任务后，在当前文档最后阅读位置插入阅读标记“# 26年1月1日 xx:xx:xx(取系统实时日期和时间) ，第x次提交(补充需求时，加上第x次补充)\n👨‍💻**提交人：${git username 运行git命令获取}**\n ## 规划任务: \n### xxxx(任务1)\n### xxxx(任务2)“；
2、任务完成后在第1条规则插入末尾插入执行结果的时间及状态，例子“## 任务处理结果: \n > ### 📄xxxx 处理结果（使用引用任务信息，给出完成内容，如果是排查问题，直接写出排查依据和结果） \n🛑**26年1月1日 xx:xx:xx 第x次提交已完成**”。 
3、完成每个任务和本文档写入后，使用“ `wc -l`当前文件行号或`tail` 当前文档尾部”的方式检查最新补充，如果存在更新或者补充要求，继续执行第1、2条规则，否则执行下一条规则；
4、每次文档更新需要遵循TDD开发模式及本文档`系统级要求`要求重新规划完成任务；规划最后一个任务必须是使用askQuestions工具Multi-select模式要求我补充需求（首选项：阅读 ${当前文档路径} 完成补充任务，如果用户选择此项表示用户在当前文档补充了新需求，需要按照要求重新读取文档内容，如果没有在尾部读取到新内容可能用户未保存，需重新调用askQuestions工具询问，Multi-select模式列出建议的后续任务），检查任务完整性不得跳过。

# 任务描述：现在当前 CJMCPAPP 项目已经初步可以使用了，能查询商品并提供商品详情页面供用户打开，我想在此基础上继续实现 刊登 和 连接 商品的功能，目前已经知道刊登功能是通过点击 商品详情页的 packages/mf-mycj-components/src/components/CjProductDetail/components/ProductInfo/Operations/index.tsx 中的 list 按钮去展开对应的弹窗内容实现的，链接功能是通过点击 商品详情页的 packages/mf-mycj-components/src/components/CjProductDetail/components/ProductInfo/Operations/index.tsx 中的 Connect 按钮去展开对应的弹窗内容实现的，现在我想通过 MCP 对话的时候，如果要想实现 刊登或者链接功能的时候，给 url 参数中增加特定的 key 和值，然后根据对应的 key 和值，在打开 url 链接的时候，继续遵循以前的逻辑“isShowList““isShowConnect“等，去默认打开对应的刊登或者链接弹窗；要实现对应的功能，需要对 mcp 和 packages/mf-mycj-components/src/components/CjProductDetail/components/ProductInfo 的内容都要修改，你先帮我生成对应的执行计划（新 md 文件），我看没问题以后再执行。
# 26年05月18日 11:23:02，第1次提交（第1次补充需求）
👨‍💻**提交人：东咸**

## 规划任务：
### 任务1：[分析] 梳理现有 list/connect 逻辑和 URL param 支持现状
### 任务2：[生成] 创建执行计划 md 文件
### 任务3：更新文档执行结果
### 任务4：askQuestions 补充需求

## 任务处理结果：

> ### 📄分析结论
>
> | 功能 | 触发方式 | 当前 URL param 支持 |
> |------|---------|-------------------|
> | 刊登(List) | `hookList.openListModal()` → postMessage 到 iframe | ✅ 已支持 `?list=1` |
> | 连接(Connect) | `hookConnect.goConnection()` → API 后跳转新页面 | ❌ 不支持 |
>
> MCP 工具现状：
> - `open_listing_page`：生成的是 `/myCJ.html#/listing?pid=...`（我的账号列表页），未使用商品详情页 + `?list=1`
> - `open_product_connect_page`：生成的是 `/myCJ.html#/productConnect`，未利用商品详情页
>
> 已生成执行计划文件：`docs/MCP-App前端接口和API整合/执行计划-刊登连接功能.md`

🛑**26年05月18日 11:23:02 第1次提交已完成**
# 26年05月18日 11:32:18，第2次提交（第2次补充需求）
👨‍💻**提交人：东咸**

## 规划任务：
### 任务1：[TDD] 先更新测试文件 navigate-tool.test.ts（新参数、新断言）
### 任务2：[实现] 修改 CJMCPAPP navigate.tool.ts（改 open_listing_page 和 open_product_connect_page）
### 任务3：[实现] 修改 mf-components use-connect.ts（新增 ?connect=1 URL param 检测）
### 任务4：[验证] 构建 CJMCPAPP + 运行全量测试通过
### 任务5：更新文档执行结果
### 任务6：askQuestions 补充需求

## 任务处理结果：

> ### 📄 刊登/连接功能实现结果
>
> **CJMCPAPP 变更（navigate.tool.ts）：**
> - `open_listing_page`：入参由 `shopId` 改为 `productName`（可选），URL 由 `/myCJ.html#/listing?pid=...` 改为商品详情页 + `?list=1`
> - `open_product_connect_page`：新增必填参数 `productId`（可选 `productName`），移除 `shopId`，URL 由 `/myCJ.html#/productConnect` 改为商品详情页 + `?connect=1`
>
> **mf-components 变更（use-connect.ts）：**
> - 新增 `removeConnectUrlQuery()` 函数，用于清理 `?connect=1` 参数防止重复触发
> - 新增 `useEffect` 检测 `?connect=1`，sku 就绪后自动调用 `goConnection()`，登录态缺失时清理参数并跳登录
>
> **测试验证：**
> - 全量测试 69/69 ✅ 全部通过
> - 构建成功（esbuild）✅
> - 测试结果文档：`测试结果/测试结果260518113514_P2刊登连接功能.md`

🛑**26年05月18日 11:36:08 第2次提交已完成**
# 26年05月18日 11:50:03，第3次提交（第3次补充需求）
👨‍💻**提交人：东咸**

## 规划任务：
### 任务1：[分析] 排查 open_product_connect_page 生成 URL 缺少商品名称和ID的原因
### 任务2：[修复] 根治原因：完善工具描述 + 加防御性检查
### 任务3：[验证] 重建并验证工具输出
### 任务4：更新文档执行结果
### 任务5：askQuestions 补充需求

## 任务处理结果：

> ### 📄 open_product_connect_page URL 缺少商品信息修复结果
>
> **根因**：AI 调用 `open_product_connect_page` 时未传入 `productId`，`getProductUrl` 生成 `/product/-p-undefined.html?connect=1`
>
> **修复内容（navigate.tool.ts）：**
> 1. 工具 description 明确要求：productId 必须来自 `search_products` 或 `query_sku_details` 返回结果，未搜索商品时需先调用 search_products
> 2. productId 参数描述标注"不得留空或传 undefined"
> 3. handler 新增防御性检查：productId 为空时返回 `isError: true` + 友好提示，引导 AI 补全信息
>
> **测试验证：**
> - 全量测试 70/70 ✅ 全部通过（新增第11个测试：缺少 productId 返回错误提示）
> - 构建成功（esbuild）✅
> - 测试结果文档：`测试结果/测试结果260518115223_P2刊登连接功能v2.md`

🛑**26年05月18日 11:52:59 第3次提交已完成**
# 26年05月18日 14:09:07，第4次提交（第4次补充需求）
👨‍💻**提交人：东咸**

## 规划任务：
### 任务1：[分析] 梳理 MCP HTTP 服务现有发布能力（ngrok/HTTPS/Docker）
### 任务2：[文档] 创建部署指南 md（ngrok 快速测试 + VPS/Docker 生产部署）
### 任务3：[代码] 添加 Dockerfile + 生产环境 npm 脚本
### 任务4：[文档] 更新 ai-client-config.md 补充 ChatGPT 远程 MCP 配置方式
### 任务5：更新文档执行结果
### 任务6：askQuestions 补充需求

## 任务处理结果：

> ### 📄 MCP 线上发布部署方案
>
> **新增文件：**
> - `docs/部署指南-线上发布供ChatGPT使用.md`：三种发布方案对比（ngrok / Docker+VPS / Railway PaaS）、完整操作步骤
> - `Dockerfile`：生产 Docker 镜像构建文件（node:20-alpine 多阶段构建，HEALTHCHECK 已配置）
>
> **package.json 新增脚本：**
> - `start:production`：生产环境启动（CJ_ENV=production）
> - `start:ngrok`：一键启动 MCP Server + ngrok（快速测试专用）
>
> **ai-client-config.md 新增：**
> - ChatGPT 远程 MCP 接入步骤（ngrok 和 VPS 两种方式）
> - 健康检查命令（`curl /health`）
>
> **快速启动命令（ngrok 方式）：**
> ```bash
> cd CJMCPAPP && npm run build && npm run start:ngrok
> # → ngrok 显示 https://xxx.ngrok-free.app
> # → 在 ChatGPT 填入 https://xxx.ngrok-free.app/mcp
> ```

🛑**26年05月18日 14:12:23 第4次提交已完成**
# 每次运行的结果插入到本段前面，以下为每次阅读时候都要确认没有遗忘的规则要求：

1、每次文档更新需要遵循TDD开发模式及本文档`系统级要求`要求重新规划完成任务；2、规划最后一个任务必须是使用askQuestions工具Multi-select模式要求我补充需求（首选项：阅读 ${当前文档路径} 完成补充任务，如果用户选择此项表示用户在当前文档补充了新需求，需要按照要求重新读取文档内容，如果没有在尾部读取到新内容可能用户未保存，需重新调用askQuestions工具询问，Multi-select模式列出建议的后续任务），检查任务完整性不得跳过。3、允许直接操作本地的软件和git管理的代码，注意操作远程会有修改、删除数据效果并且不可撤销的操作必须先写入完整的操作方案，然后调用askQuestions工具Multi-select模式询问，确认后按照方案操作。4、当出现纠正的时候需要在修改的每个代码文件、方法前面按照规范写入注意事项注释说明，比如java代码需要遵循java doc注释规范，js代码需要遵循js doc注释规范；阅读代码时需要注意这些注释说明，理解修改的原因和目的；5、及时清理掉无用的文件，无用的日志文件，临时文件保存到tmp目录；6、注意检查保密、密钥等信息不要加入git管理，如果存在提示我需要删除；